<%= render 'shared/navbar_show' %>
<iframe src=<%= @article.source_url%> id="IFRAME_ID"></iframe>
<%puts "hello"%>
<%list = List.find_by(name: "History")%>

<%if !list.articles.include? @article %> 
  <%Bookmark.create!(article_id: @article.id, list_id: list.id)%>
<%end%>
