<%= render 'shared/navbar_show' %>
<iframe src=<%= @article.source_url%> id="IFRAME_ID"></iframe>

<%history_list = current_user.lists[0]%>

<%if !history_list.articles.include? @article %>
  <%Bookmark.create!(article_id: @article.id, list_id: history_list.id)%>
<%end%>
